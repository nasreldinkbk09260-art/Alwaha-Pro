{
  "name": "alwaha-pro",
  "version": "2.7.0",
  "description": "تطبيق واحة المحترفين الموحد",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && cp icon.png www/ || true"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
