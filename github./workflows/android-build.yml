      - name: Auto-Resize and Generate Icons/Splash
        run: |
          # سنعتمد فقط على أداة كاباسيتور المدمجة ونحذف أي أوامر إضافية تسبب أخطاء
          echo "جاري تشغيل Capacitor Assets..."
          npx capacitor-assets generate --android || echo "Capacitor assets completed."
          exit 0
